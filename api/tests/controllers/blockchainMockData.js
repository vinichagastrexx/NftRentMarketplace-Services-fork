const newBlockchainData = {
  name: 'chainName',
  currency: 'bcn',
}

const blockchainData = {
  id: 1,
  name: 'chainName',
  currency: 'bcn',
}

const otherBlockchainData = {
  id: 2,
  name: 'otherChain',
  currency: 'obc',
}

const requiredFields = ['name', 'currency'];

module.exports = {newBlockchainData, blockchainData, otherBlockchainData, requiredFields}